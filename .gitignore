# ignore some stuff generated by emacs:
*~
\#*\#
*.#*
/.emacs.desktop
/.emacs.desktop.lock

# ignore generated files in the doc directory
doc/*.tex
doc/*.pdf

# don't know what these files are, but we don't want them in the repo:
*.whl